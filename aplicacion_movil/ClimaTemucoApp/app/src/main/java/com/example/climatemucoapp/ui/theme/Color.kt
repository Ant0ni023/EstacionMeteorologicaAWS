package com.example.climatemucoapp.ui.theme

import androidx.compose.ui.graphics.Color

// Colores primarios
val Primary = Color(0xFF1976D2)      // Azul principal
val OnPrimary = Color(0xFFFFFFFF)    // Texto sobre el color primario
val PrimaryContainer = Color(0xFFD1E4FF) // Contenedor primario
val OnPrimaryContainer = Color(0xFF001D36) // Texto sobre el contenedor primario

// Colores secundarios
val Secondary = Color(0xFF00796B)     // Verde azulado
val OnSecondary = Color(0xFFFFFFFF)   // Texto sobre el color secundario
val SecondaryContainer = Color(0xFFB2DFDB) // Contenedor secundario
val OnSecondaryContainer = Color(0xFF002021) // Texto sobre el contenedor secundario

// Colores de fondo y superficie
val Background = Color(0xFFFAFAFA)    // Fondo general
val OnBackground = Color(0xFF1A1C1E)  // Texto sobre el fondo
val Surface = Color(0xFFFFFFFF)       // Superficie de tarjetas
val OnSurface = Color(0xFF1A1C1E)     // Texto sobre superficie
val SurfaceVariant = Color(0xFFE7E0EC) // Variante de superficie
val OnSurfaceVariant = Color(0xFF49454F) // Texto sobre variante de superficie

// Colores de error
val Error = Color(0xFFB00020)         // Color de error
val OnError = Color(0xFFFFFFFF)       // Texto sobre error
val ErrorContainer = Color(0xFFFFDAD6) // Contenedor de error
val OnErrorContainer = Color(0xFF410002) // Texto sobre contenedor de error

// Colores adicionales para la aplicación del clima
val TemperatureWarm = Color(0xFFFF6B6B)  // Para temperaturas altas
val TemperatureCold = Color(0xFF4ECDC4)  // Para temperaturas bajas
val HumidityColor = Color(0xFF45B7D1)    // Para humedad
val PressureColor = Color(0xFF96BAFF)    // Para presión
val RainColor = Color(0xFF6C88C4)        // Para probabilidad de lluvia